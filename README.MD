## Installation
```bash
git clone https://github.com/z4pdy/dotfiles
cd dotfiles
mkdir -p nvim/.config
git clone https://github.com/z4pdy/nvim-config nvim/.config/nvim
chmod +x scripts/.local/bin/*
```

waybar:
yay -S rofi-bluetooth-git
yay -S networkmanager-dmenu
